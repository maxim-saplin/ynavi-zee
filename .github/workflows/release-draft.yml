# Deprecated placeholder — use workflow "release" (workflow_dispatch) instead.
name: release-draft-placeholder
on:
  workflow_dispatch:
jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "::error::Use Actions → release (workflow_dispatch, tag default ynavi-zeekr-v27.0.2). This placeholder is retired."
          exit 1
