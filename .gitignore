# Local iteration artifacts (do not commit)
builds/
features/config.env
src/build/

# APK artifacts — never commit binaries (Releases / builds/ only)
**/*.apk
modded_apks/

# Common OS noise
.DS_Store


tmp/
yandex-navigator*.apk
Huawei*.apk
